<!DOCTYPE html>
<html lang="en">
<head>
  <title><%= title %></title>
  <%- include('meta-tags', { robots: robots }); %>
  <% if (typeof css !== 'undefined') { %>
    <% css.forEach(function(bundle) { %>
    <% if (bundle) { %>
    <link rel="stylesheet" href="/<%= bundle %>" />
    <% } %>
    <% }) %>
  <% } %>
  <style>
    :root {
      /* @todo make sure that these colors match the theme in _colors.scss */
      --body-background: hsl(0, 0%, 99%);
      --body-color: hsl(0, 0%, 20%);
      --skeleton-border-color: hsl(0, 0%, 95%);
      --skeleton-background-color: hsl(0, 0%, 99%);
      --skeleton-flash-color: hsla(0, 0%, 90%, .5);
      --logo-text-color: hsl(0, 0%, 99%);
    }
    @media screen and (prefers-color-scheme: dark) {
      :root {
        --body-background: hsl(220, 7%, 16%);
        --body-color: rgba(255, 255, 255, 85%);
        --skeleton-border-color: hsl(218, 7%, 24%);
        --skeleton-background-color: hsl(223, 7%, 19%);
        --skeleton-flash-color: hsl(218, 7%, 24%);
        --logo-text-color: hsl(220, 7%, 16%);
      }
    }
    * { box-sizing: border-box; }
    body {
      background-color: var(--body-background);
      color: var(--body-color);
      margin: 0;
      min-height: 100vh;
      overflow-x: hidden;
      padding: 0;
    }
    .masthead {
      border-top: 6px solid hsl(17, 93%, 65%);
      height: 71px;
    }
    .navbar {
      align-items: center;
      border-bottom: thin solid var(--skeleton-border-color);
      display: flex;
      height: 64px;
      justify-content: space-between;
    }
    .navbar__icon {
      background-color: var(--skeleton-background-color);
      border-radius: 50%;
      height: 40px;
      overflow: hidden;
      width: 40px;
    }
    .navbar__brand {
      align-items: center;
      display: flex;
      height: 100%;
      justify-content: center;
      width: 64px;
    }
      .navbar__brand .skeleton {
        animation-delay: 0.3s;
      }
    .navbar__user {
      align-items: center;
      border-left: thin solid var(--skeleton-background-color);
      height: 100%;
      display: flex;
      justify-content: center;
      width: 64px;
    }
      .navbar__user .skeleton {
        animation-delay: 0.5s;
      }
    .navbar__content {
      align-items: center;
      display: flex;
      flex: 1;
    }
      .navbar__content .skeleton {
        animation-delay: 0.8s;
        width: 200px;
      }
    .logo-bg {
      animation: logo 5s linear infinite;
      fill: #e8eae8;
    }
    .skeleton{
      animation: loading 5s linear infinite;
      background-repeat: no-repeat;
      background: linear-gradient(90deg, var(--body-background), var(--skeleton-flash-color), var(--body-background));
      background-size: 300% 300%;
      display: block;
      height: 100%;
      position: relative;
      width: 100%;
    }
      .skeleton.line {
        height: 16px;
      }
    @keyframes loading {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 51%; }
      100% { background-position: 0% 50%; }
    }
    .logo {
      align-items: center;
      display: flex;
      flex-direction: column;
      height: calc(100vh - 71px);
      justify-content: center;
      width: 100%;
    }
      .logo-p {
        fill: var(--logo-text-color);
      }
    @keyframes logo {
      0% { fill: var(--skeleton-flash-color); }
      50% { fill: var(--body-background); }
      100% { fill: var(--skeleton-flash-color); }
    }
  </style>
  <link rel="preload" href="/css/fonts/domine-v7-latin-regular.woff2" as="font">
  <link rel="preload" href="/css/fonts/domine-v7-latin-700.woff2" as="font">
</head>
<body class="<% if (typeof permalink !== 'undefined') { %><%= permalink %><% } %>">
