import React from 'react'
import PropTypes from 'prop-types'

import IconImage from '../../../images/icon/image.svg'

const FieldImageNew = ({ props }) => {
  return (
    <div className="document-field__image document-field__image--new">
      <IconImage />
    </div>
  )
}

FieldImageNew.propTypes = {
}

export default FieldImageNew
