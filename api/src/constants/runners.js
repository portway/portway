// Constants used by workerFactory and worker_threads to execute tasks.
// Key is the constant name, value should match the filename in
// libs/workers/runners
export default Object.freeze({
  MARKDOWN_RUNNER: 'markdownParser'
})